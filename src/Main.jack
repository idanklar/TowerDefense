// Main entry point: creates the game and starts the main loop.
class Main {
    // Program entry point.
    function void main() {
        var Game g;
        let g = Game.new();
        do g.run();
        do g.dispose();
        return;
    }
}
